{
  "name": "@request-indexing/edge",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "DNS-level edge Worker — observes AI crawler hits and forwards everything else to origin. The smallest possible proof of the VISION.md thesis.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply request-indexing-edge",
    "db:migrate:local": "wrangler d1 migrations apply request-indexing-edge --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
